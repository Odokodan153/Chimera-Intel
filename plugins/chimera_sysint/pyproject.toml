[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chimera_sysint"
version = "0.1.0"
dependencies = [
    "chimera-intel",
    "networkx",
    "python-louvain"
]