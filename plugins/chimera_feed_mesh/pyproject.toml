[tool.poetry]
name = "chimera_feed_mesh"
version = "0.1.0"
description = "Plugin for real-time threat feed integrations (GreyNoise, Certstream, etc.)"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
chimera_intel = {path = "../../src", develop = true}
typer = "^0.9.0"
websockets = "^12.0"
httpx = "^0.27.0"

[tool.poetry.plugins."chimera_intel.plugins"]
feed_mesh = "chimera_feed_mesh.main:FeedMeshPlugin"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"