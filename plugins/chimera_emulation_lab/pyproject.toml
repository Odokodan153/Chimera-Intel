[tool.poetry]
name = "chimera-emulation-lab"
version = "0.1.0"
description = "Threat Actor Emulation Lab plugin for Chimera Intel."
authors = [{ name = "Ignacio Iliev", email = "ignasioi72@gmail.com" }]

[tool.poetry.dependencies]
python = "^3.10"
# This path assumes the plugin is two directories deep from the project root
chimera-intel = {path = "../../", develop = true}
# Dependency for the real lab module
docker = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"