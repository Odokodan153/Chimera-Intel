[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chimera_spaceint"
version = "0.1.0"
dependencies = [
    "chimera-intel",
]

[project.entry-points."chimera_intel.plugins"]
spaceint = "chimera_spaceint.main:app"