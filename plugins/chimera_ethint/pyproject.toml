[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chimera_ethint"
version = "0.1.0"
authors = [
    { name = "Ignacio Iliev", email = "ignasioi72@gmail.com" },
]
description = "An incorruptible, logic-based supervisor to ensure all platform operations are wielded responsibly."
dependencies = [
    "chimera-intel",
]