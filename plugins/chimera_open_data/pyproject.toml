[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chimera-open-data"
version = "1.0.0"
description = "A plugin for Open-Source Data Intelligence (OS-DATAINT) using datasets like the World Bank."
requires-python = ">=3.9"
dependencies = [
    "chimera-intel", 
]